openapi: 3.1.0
info:
  title: Club Event & Fee Management API
  version: 0.1.0
  description: 교내 개발 동아리 행사·회비 관리 웹앱의 첫 번째 세로 기능 API 계약
servers:
  - url: /api/v1
security:
  - cookieAuth: []
tags:
  - name: Me
  - name: Events
  - name: Payments
  - name: Admin
paths:
  /auth/csrf:
    get:
      tags: [Auth]
      operationId: getCsrfToken
      summary: 상태 변경 요청용 CSRF 토큰 발급
      responses:
        '200':
          description: CSRF 토큰
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CsrfResponse'
  /me:
    get:
      tags: [Me]
      operationId: getMe
      summary: 현재 사용자 조회
      responses:
        '200':
          description: 현재 사용자
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /me/onboarding:
    patch:
      tags: [Me]
      operationId: completeOnboarding
      summary: 최초 온보딩 완료
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OnboardingRequest'
      responses:
        '200':
          description: 온보딩 완료 사용자
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /me/profile:
    patch:
      tags: [Me]
      operationId: updateMyProfile
      summary: 내 프로필 수정
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OnboardingRequest'
      responses:
        '200':
          description: 수정된 사용자
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
  /events:
    get:
      tags: [Events]
      operationId: getEvents
      summary: 게시 행사 목록 조회
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
        - name: participating
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: 행사 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventPage'
        '403':
          $ref: '#/components/responses/Forbidden'
  /events/{eventId}:
    get:
      tags: [Events]
      operationId: getEvent
      summary: 행사 상세 조회
      parameters:
        - $ref: '#/components/parameters/EventId'
      responses:
        '200':
          description: 행사 상세
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventDetail'
        '404':
          $ref: '#/components/responses/NotFound'
  /events/{eventId}/participation:
    post:
      tags: [Events]
      operationId: joinEvent
      summary: 행사 참가 신청
      parameters:
        - $ref: '#/components/parameters/EventId'
      responses:
        '201':
          description: 참가 신청 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JoinEventResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /events/{eventId}/participation/cancel:
    post:
      tags: [Events]
      operationId: cancelEventParticipation
      summary: 행사 참가 취소
      parameters:
        - $ref: '#/components/parameters/EventId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelParticipationRequest'
      responses:
        '200':
          description: 참가 취소 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CancelParticipationResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /me/payment-obligations:
    get:
      tags: [Payments]
      operationId: getMyPaymentObligations
      summary: 내 납부 항목 목록
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
        - name: status
          in: query
          schema:
            type: array
            items:
              $ref: '#/components/schemas/PaymentStatus'
      responses:
        '200':
          description: 납부 항목 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentPage'
  /payment-obligations/{paymentId}:
    get:
      tags: [Payments]
      operationId: getPaymentObligation
      summary: 내 납부 상세 조회
      parameters:
        - $ref: '#/components/parameters/PaymentId'
      responses:
        '200':
          description: 납부 상세
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentDetail'
        '404':
          $ref: '#/components/responses/NotFound'
  /payment-obligations/{paymentId}/reports:
    post:
      tags: [Payments]
      operationId: reportPayment
      summary: 송금 완료 신고
      parameters:
        - $ref: '#/components/parameters/PaymentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentReportRequest'
      responses:
        '201':
          description: 접수된 송금 신고
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentReportResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/dashboard:
    get:
      tags: [Admin]
      operationId: getAdminDashboard
      summary: 운영진 대시보드 조회
      responses:
        '200':
          description: 운영 현황 요약
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDashboardResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
  /notifications:
    get:
      tags: [Notifications]
      operationId: getNotifications
      summary: 내 알림 목록과 읽지 않은 개수 조회
      responses:
        '200':
          description: 내 알림
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationResponse'
  /notifications/{notificationId}/read:
    post:
      tags: [Notifications]
      operationId: readNotification
      summary: 알림 읽음 처리
      parameters:
        - $ref: '#/components/parameters/NotificationId'
      responses:
        '200':
          description: 읽은 알림
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationItem'
  /notifications/read-all:
    post:
      tags: [Notifications]
      operationId: readAllNotifications
      summary: 모든 알림 읽음 처리
      responses:
        '204':
          description: 처리 완료
  /admin/events:
    get:
      tags: [Admin]
      operationId: getAdminEvents
      summary: 운영진 행사 전체 목록 조회
      responses:
        '200':
          description: 행사 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminEventResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Admin]
      operationId: createAdminEvent
      summary: 행사 초안 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventCreateRequest'
      responses:
        '201':
          description: 생성된 행사 초안
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
  /admin/events/{eventId}:
    get:
      tags: [Admin]
      operationId: getAdminEvent
      summary: 운영진 행사 상세 조회
      parameters:
        - $ref: '#/components/parameters/EventId'
      responses:
        '200':
          description: 행사 상세
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventResponse'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Admin]
      operationId: updateAdminEvent
      summary: 행사 초안 수정
      parameters:
        - $ref: '#/components/parameters/EventId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventUpdateRequest'
      responses:
        '200':
          description: 수정된 행사
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventResponse'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      tags: [Admin]
      operationId: deleteAdminEvent
      summary: 참가 이력이 없는 행사 초안 삭제
      parameters:
        - $ref: '#/components/parameters/EventId'
        - name: version
          in: query
          required: true
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '204':
          description: 삭제 완료
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/events/{eventId}/publish:
    post:
      tags: [Admin]
      operationId: publishAdminEvent
      summary: 행사 초안 공개
      parameters:
        - $ref: '#/components/parameters/EventId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventVersionRequest'
      responses:
        '200':
          description: 공개된 행사
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/events/{eventId}/close:
    post:
      tags: [Admin]
      operationId: closeAdminEvent
      summary: 공개 행사 종료
      parameters:
        - $ref: '#/components/parameters/EventId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventTransitionRequest'
      responses:
        '200':
          description: 종료된 행사
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/events/{eventId}/cancel:
    post:
      tags: [Admin]
      operationId: cancelAdminEvent
      summary: 행사 취소 및 납부 상태 정리
      parameters:
        - $ref: '#/components/parameters/EventId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventTransitionRequest'
      responses:
        '200':
          description: 행사 취소 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventCancelResponse'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/events/{eventId}/participants:
    get:
      tags: [Admin]
      operationId: getAdminEventParticipants
      summary: 행사별 참가자와 참가비 납부 현황 조회
      parameters:
        - $ref: '#/components/parameters/EventId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
        - name: paymentStatus
          in: query
          schema:
            $ref: '#/components/schemas/PaymentStatus'
      responses:
        '200':
          description: 행사 참가자 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminEventParticipantPage'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /admin/dues-rounds/{duesRoundId}/payments:
    get:
      tags: [Admin]
      operationId: getAdminDuesPayments
      summary: 회비 차수별 부원 납부 현황 조회
      parameters:
        - $ref: '#/components/parameters/DuesRoundId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
        - name: paymentStatus
          in: query
          schema:
            $ref: '#/components/schemas/PaymentStatus'
      responses:
        '200':
          description: 회비 납부 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDuesPaymentPage'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /admin/dues-rounds:
    get:
      tags: [Admin]
      operationId: getAdminDuesRounds
      summary: 회비 차수 목록 조회
      responses:
        '200':
          description: 회비 차수 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminDuesRoundResponse'
    post:
      tags: [Admin]
      operationId: createAdminDuesRound
      summary: 회비 차수 초안 생성
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminDuesRoundRequest'
      responses:
        '201':
          description: 생성된 회비 차수
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDuesRoundResponse'
  /admin/dues-rounds/{duesRoundId}:
    patch:
      tags: [Admin]
      operationId: updateAdminDuesRound
      summary: 회비 차수 초안 수정
      parameters:
        - $ref: '#/components/parameters/DuesRoundId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminDuesRoundRequest'
      responses:
        '200':
          description: 수정된 회비 차수
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDuesRoundResponse'
    delete:
      tags: [Admin]
      operationId: deleteAdminDuesRound
      summary: 회비 차수 초안 삭제
      parameters:
        - $ref: '#/components/parameters/DuesRoundId'
        - name: version
          in: query
          required: true
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '204':
          description: 삭제 완료
  /admin/dues-rounds/{duesRoundId}/publish:
    post:
      tags: [Admin]
      operationId: publishAdminDuesRound
      summary: 회비 차수 공개 및 활성 부원 납부 항목 생성
      parameters:
        - $ref: '#/components/parameters/DuesRoundId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminEventVersionRequest'
      responses:
        '200':
          description: 공개 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDuesPublishResponse'
  /admin/payment-obligations/{paymentId}/confirm:
    post:
      tags: [Admin]
      operationId: confirmAdminPayment
      summary: 송금 신고 승인
      parameters:
        - $ref: '#/components/parameters/PaymentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPaymentReviewRequest'
      responses:
        '200':
          description: 승인된 납부 상태
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPaymentReviewResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/payment-obligations/{paymentId}/reject:
    post:
      tags: [Admin]
      operationId: rejectAdminPayment
      summary: 송금 신고 반려
      parameters:
        - $ref: '#/components/parameters/PaymentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPaymentReviewRequest'
      responses:
        '200':
          description: 반려된 납부 상태
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPaymentReviewResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/refunds:
    get:
      tags: [Admin]
      operationId: getAdminRefunds
      summary: 환불 대기 목록 조회
      responses:
        '200':
          description: 환불 대기 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminPaymentRow'
  /admin/payment-obligations/{paymentId}/refund:
    post:
      tags: [Admin]
      operationId: completeAdminRefund
      summary: 환불 완료 처리
      parameters:
        - $ref: '#/components/parameters/PaymentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPaymentReviewRequest'
      responses:
        '200':
          description: 환불 완료 상태
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPaymentReviewResponse'
  /admin/members:
    get:
      tags: [Admin]
      operationId: getAdminMembers
      summary: 회원 목록 조회
      responses:
        '200':
          description: 회원 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminMemberResponse'
  /admin/members/{memberId}/approve:
    post:
      tags: [Admin]
      operationId: approveAdminMember
      summary: 가입 대기 회원 승인
      parameters:
        - $ref: '#/components/parameters/MemberId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminMemberActionRequest'
      responses:
        '200':
          description: 승인된 회원
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMemberResponse'
  /admin/members/{memberId}/suspend:
    post:
      tags: [Admin]
      operationId: suspendAdminMember
      summary: 활성 회원 정지
      parameters:
        - $ref: '#/components/parameters/MemberId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminMemberActionRequest'
      responses:
        '200':
          description: 정지된 회원
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMemberResponse'
  /admin/members/{memberId}/role:
    patch:
      tags: [Admin]
      operationId: changeAdminMemberRole
      summary: 활성 회원 역할 변경
      parameters:
        - $ref: '#/components/parameters/MemberId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminMemberRoleRequest'
      responses:
        '200':
          description: 역할이 변경된 회원
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMemberResponse'
  /admin/payment-settings:
    get:
      tags: [Admin]
      operationId: getAdminPaymentSettings
      summary: 송금정보 버전 목록
      responses:
        '200':
          description: 송금정보 버전 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PaymentSettingResponse'
    post:
      tags: [Admin]
      operationId: createAdminPaymentSetting
      summary: 새 송금정보 등록 및 활성화
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentSettingRequest'
      responses:
        '201':
          description: 활성화된 송금정보
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentSettingResponse'
  /admin/payment-settings/active:
    get:
      tags: [Admin]
      operationId: getActiveAdminPaymentSetting
      summary: 현재 활성 송금정보 조회
      responses:
        '200':
          description: 현재 활성 송금정보
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentSettingResponse'
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: SESSION
  parameters:
    EventId:
      name: eventId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    PaymentId:
      name: paymentId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    DuesRoundId:
      name: duesRoundId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    MemberId:
      name: memberId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    NotificationId:
      name: notificationId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    Page:
      name: page
      in: query
      schema:
        type: integer
        minimum: 0
        default: 0
    Size:
      name: size
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
  responses:
    BadRequest:
      description: 잘못된 요청
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Unauthorized:
      description: 로그인 필요
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Forbidden:
      description: 접근 권한 없음
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: 리소스 없음
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Conflict:
      description: 현재 상태와 요청 충돌
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    CsrfResponse:
      type: object
      required: [headerName, parameterName, token]
      properties:
        headerName:
          type: string
        parameterName:
          type: string
        token:
          type: string
    MemberPart:
      type: string
      enum: [PLAN, DESIGN, PE_WEB, PE_MOBILE]
    MemberRole:
      type: string
      enum: [MEMBER, STAFF, ADMIN]
    MemberStatus:
      type: string
      enum: [PENDING, ACTIVE, SUSPENDED, WITHDRAWN]
    EventStatus:
      type: string
      enum: [DRAFT, PUBLISHED, CLOSED, CANCELED]
    ParticipationStatus:
      type: string
      enum: [JOINED, CANCELED]
    PaymentType:
      type: string
      enum: [EVENT_FEE, MEMBERSHIP_DUE]
    PaymentStatus:
      type: string
      enum: [NOT_REQUIRED, UNPAID, REPORTED, CONFIRMED, REJECTED, VOID, REFUND_PENDING, REFUNDED]
    PaymentMethod:
      type: string
      enum: [BANK_TRANSFER, KAKAO_PAY_CODE]
    MeResponse:
      type: object
      required: [id, kakaoProfileName, role, status, onboardingCompleted]
      properties:
        id:
          type: integer
          format: int64
        kakaoProfileName:
          type: string
        name:
          type: string
        part:
          $ref: '#/components/schemas/MemberPart'
        displayNickname:
          type: string
        role:
          $ref: '#/components/schemas/MemberRole'
        status:
          $ref: '#/components/schemas/MemberStatus'
        onboardingCompleted:
          type: boolean
        approvedAt:
          type: string
          format: date-time
    OnboardingRequest:
      type: object
      required: [name, part]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 50
        part:
          $ref: '#/components/schemas/MemberPart'
    EventListItem:
      type: object
      required: [id, title, startsAt, registrationDeadline, joinedCount, feeAmount, status]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        summary:
          type: string
        location:
          type: string
        startsAt:
          type: string
          format: date-time
        endsAt:
          type: string
          format: date-time
        registrationDeadline:
          type: string
          format: date-time
        capacity:
          type: integer
        joinedCount:
          type: integer
        feeAmount:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/EventStatus'
        myParticipationStatus:
          $ref: '#/components/schemas/ParticipationStatus'
        myPaymentStatus:
          $ref: '#/components/schemas/PaymentStatus'
    EventPage:
      type: object
      required: [items, page, size, totalElements, totalPages]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EventListItem'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer
    ParticipationSummary:
      type: object
      required: [id, status, joinedAt, version]
      properties:
        id:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/ParticipationStatus'
        joinedAt:
          type: string
          format: date-time
        canceledAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    PaymentSummary:
      type: object
      required: [id, type, amount, status, source, updatedAt, version]
      properties:
        id:
          type: integer
          format: int64
        type:
          $ref: '#/components/schemas/PaymentType'
        amount:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/PaymentStatus'
        dueAt:
          type: string
          format: date-time
        source:
          $ref: '#/components/schemas/PaymentSource'
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    PaymentSource:
      type: object
      required: [type, id, title]
      properties:
        type:
          type: string
          enum: [EVENT, DUES_ROUND]
        id:
          type: integer
          format: int64
        title:
          type: string
        dueAt:
          type: string
          format: date-time
    EventDetail:
      allOf:
        - $ref: '#/components/schemas/EventListItem'
        - type: object
          required: [description, allowLateCancellation, canJoin, canCancel]
          properties:
            description:
              type: string
            allowLateCancellation:
              type: boolean
            canJoin:
              type: boolean
            canCancel:
              type: boolean
            myParticipation:
              $ref: '#/components/schemas/ParticipationSummary'
            myPayment:
              $ref: '#/components/schemas/PaymentSummary'
    JoinEventResponse:
      type: object
      required: [participation, payment]
      properties:
        participation:
          $ref: '#/components/schemas/ParticipationSummary'
        payment:
          $ref: '#/components/schemas/PaymentSummary'
    CancelParticipationRequest:
      type: object
      required: [version]
      properties:
        version:
          type: integer
          format: int64
        reason:
          type: string
          maxLength: 300
    CancelParticipationResponse:
      type: object
      required: [participationStatus, paymentStatus, refundRequired]
      properties:
        participationStatus:
          $ref: '#/components/schemas/ParticipationStatus'
        paymentStatus:
          $ref: '#/components/schemas/PaymentStatus'
        refundRequired:
          type: boolean
    PaymentPage:
      type: object
      required: [items, page, size, totalElements, totalPages]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PaymentSummary'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer
    PaymentDestination:
      type: object
      required: [bankName, accountNumber, accountHolder]
      properties:
        bankName:
          type: string
        accountNumber:
          type: string
        accountHolder:
          type: string
        kakaoPayReceiveUrl:
          type: string
          format: uri
    PaymentReport:
      type: object
      required: [id, method, senderName, reportedAt]
      properties:
        id:
          type: integer
          format: int64
        method:
          $ref: '#/components/schemas/PaymentMethod'
        senderName:
          type: string
        transferredAt:
          type: string
          format: date-time
        note:
          type: string
        reportedAt:
          type: string
          format: date-time
    PaymentStatusHistory:
      type: object
      required: [toStatus, changedAt]
      properties:
        fromStatus:
          $ref: '#/components/schemas/PaymentStatus'
        toStatus:
          $ref: '#/components/schemas/PaymentStatus'
        reason:
          type: string
        changedAt:
          type: string
          format: date-time
    PaymentDetail:
      allOf:
        - $ref: '#/components/schemas/PaymentSummary'
        - type: object
          required: [statusHistory]
          properties:
            paymentDestination:
              $ref: '#/components/schemas/PaymentDestination'
            latestReport:
              $ref: '#/components/schemas/PaymentReport'
            statusHistory:
              type: array
              items:
                $ref: '#/components/schemas/PaymentStatusHistory'
    PaymentReportRequest:
      type: object
      required: [method, senderName, transferConfirmed, version]
      properties:
        method:
          $ref: '#/components/schemas/PaymentMethod'
        senderName:
          type: string
          minLength: 1
          maxLength: 100
        transferredAt:
          type: string
          format: date-time
        note:
          type: string
          maxLength: 500
        transferConfirmed:
          type: boolean
          const: true
        version:
          type: integer
          format: int64
    PaymentReportResponse:
      type: object
      required: [report, paymentStatus, version]
      properties:
        report:
          $ref: '#/components/schemas/PaymentReport'
        paymentStatus:
          $ref: '#/components/schemas/PaymentStatus'
        version:
          type: integer
          format: int64
    AdminEventCreateRequest:
      type: object
      required: [title, description, startsAt, registrationDeadline, feeAmount, allowLateCancellation]
      properties:
        title:
          type: string
          maxLength: 200
        summary:
          type: string
          maxLength: 500
        description:
          type: string
        location:
          type: string
          maxLength: 200
        startsAt:
          type: string
          format: date-time
        endsAt:
          type: string
          format: date-time
        registrationDeadline:
          type: string
          format: date-time
        capacity:
          type: integer
          minimum: 1
        feeAmount:
          type: integer
          format: int64
          minimum: 0
        allowLateCancellation:
          type: boolean
    AdminEventUpdateRequest:
      allOf:
        - $ref: '#/components/schemas/AdminEventCreateRequest'
        - type: object
          required: [version]
          properties:
            version:
              type: integer
              format: int64
              minimum: 0
    AdminEventVersionRequest:
      type: object
      required: [version]
      properties:
        version:
          type: integer
          format: int64
          minimum: 0
    AdminEventTransitionRequest:
      type: object
      required: [version]
      properties:
        version:
          type: integer
          format: int64
          minimum: 0
        reason:
          type: string
          maxLength: 500
    AdminEventCancelResponse:
      type: object
      required: [eventStatus, voidedPaymentCount, refundPendingCount, version]
      properties:
        eventStatus:
          $ref: '#/components/schemas/EventStatus'
        voidedPaymentCount:
          type: integer
          format: int64
        refundPendingCount:
          type: integer
          format: int64
        version:
          type: integer
          format: int64
    AdminEventResponse:
      type: object
      required: [id, title, description, startsAt, registrationDeadline, joinedCount, feeAmount, status, allowLateCancellation, createdAt, updatedAt, version]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        summary:
          type: string
        description:
          type: string
        location:
          type: string
        startsAt:
          type: string
          format: date-time
        endsAt:
          type: string
          format: date-time
        registrationDeadline:
          type: string
          format: date-time
        capacity:
          type: integer
        joinedCount:
          type: integer
          format: int64
        feeAmount:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/EventStatus'
        allowLateCancellation:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    AdminDashboardResponse:
      type: object
      required: [memberCount, pendingMemberCount, upcomingEventCount, unpaidCount, reportedCount, expectedAmount, confirmedAmount, collectionRate, upcomingEvents, activeDuesRounds, recentReports]
      properties:
        memberCount:
          type: integer
        pendingMemberCount:
          type: integer
        upcomingEventCount:
          type: integer
        unpaidCount:
          type: integer
        reportedCount:
          type: integer
        expectedAmount:
          type: integer
          format: int64
        confirmedAmount:
          type: integer
          format: int64
        collectionRate:
          type: number
          format: double
          minimum: 0
          maximum: 100
        upcomingEvents:
          type: array
          items:
            $ref: '#/components/schemas/AdminEventSummary'
        activeDuesRounds:
          type: array
          items:
            $ref: '#/components/schemas/AdminDuesRoundSummary'
        recentReports:
          type: array
          items:
            $ref: '#/components/schemas/AdminPaymentRow'
    AdminEventSummary:
      type: object
      required: [id, title, startsAt, joinedCount, feeAmount, unpaidCount, reportedCount, confirmedCount]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        startsAt:
          type: string
          format: date-time
        capacity:
          type: integer
        joinedCount:
          type: integer
        feeAmount:
          type: integer
          format: int64
        unpaidCount:
          type: integer
        reportedCount:
          type: integer
        confirmedCount:
          type: integer
    AdminDuesRoundSummary:
      type: object
      required: [id, title, amount, dueAt, targetCount, unpaidCount, reportedCount, confirmedCount, confirmedAmount]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        amount:
          type: integer
          format: int64
        dueAt:
          type: string
          format: date-time
        targetCount:
          type: integer
        unpaidCount:
          type: integer
        reportedCount:
          type: integer
        confirmedCount:
          type: integer
        confirmedAmount:
          type: integer
          format: int64
    AdminDuesRoundStatus:
      type: string
      enum: [DRAFT, PUBLISHED, CLOSED]
    AdminDuesRoundRequest:
      type: object
      required: [title, amount, dueAt, version]
      properties:
        title:
          type: string
          maxLength: 200
        amount:
          type: integer
          format: int64
          minimum: 0
        dueAt:
          type: string
          format: date-time
        bankName:
          type: string
          maxLength: 100
        accountNumber:
          type: string
          maxLength: 100
        accountHolder:
          type: string
          maxLength: 100
        kakaoPayReceiveUrl:
          type: string
          maxLength: 500
        version:
          type: integer
          format: int64
          minimum: 0
    AdminDuesRoundResponse:
      type: object
      required: [id, title, amount, dueAt, status, targetCount, createdAt, updatedAt, version]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        amount:
          type: integer
          format: int64
        dueAt:
          type: string
          format: date-time
        status:
          $ref: '#/components/schemas/AdminDuesRoundStatus'
        bankName:
          type: string
        accountNumber:
          type: string
        accountHolder:
          type: string
        kakaoPayReceiveUrl:
          type: string
        targetCount:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    AdminDuesPublishResponse:
      type: object
      required: [duesRound, createdPaymentCount]
      properties:
        duesRound:
          $ref: '#/components/schemas/AdminDuesRoundResponse'
        createdPaymentCount:
          type: integer
          format: int64
    AdminPaymentRow:
      type: object
      required: [paymentId, memberId, nickname, name, part, amount, status, dueAt, version]
      properties:
        paymentId:
          type: integer
          format: int64
        memberId:
          type: integer
          format: int64
        nickname:
          type: string
        name:
          type: string
        part:
          $ref: '#/components/schemas/MemberPart'
        amount:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/PaymentStatus'
        dueAt:
          type: string
          format: date-time
        latestReport:
          $ref: '#/components/schemas/PaymentReport'
        version:
          type: integer
          format: int64
    AdminEventParticipant:
      type: object
      required: [participationId, memberId, nickname, name, part, joinedAt, amount, paymentStatus, version]
      properties:
        participationId:
          type: integer
          format: int64
        memberId:
          type: integer
          format: int64
        nickname:
          type: string
        name:
          type: string
        part:
          $ref: '#/components/schemas/MemberPart'
        joinedAt:
          type: string
          format: date-time
        paymentId:
          type: integer
          format: int64
        amount:
          type: integer
          format: int64
        paymentStatus:
          $ref: '#/components/schemas/PaymentStatus'
        latestReport:
          $ref: '#/components/schemas/PaymentReport'
        version:
          type: integer
          format: int64
    AdminEventParticipantPage:
      type: object
      required: [event, items, page, size, totalElements, totalPages]
      properties:
        event:
          $ref: '#/components/schemas/AdminEventSummary'
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminEventParticipant'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer
    AdminDuesPaymentPage:
      type: object
      required: [duesRound, items, page, size, totalElements, totalPages]
      properties:
        duesRound:
          $ref: '#/components/schemas/AdminDuesRoundSummary'
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminPaymentRow'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer
    AdminPaymentReviewRequest:
      type: object
      required: [version]
      properties:
        version:
          type: integer
          format: int64
        note:
          type: string
          maxLength: 500
    AdminPaymentReviewResponse:
      type: object
      required: [paymentId, status, version, reviewedAt]
      properties:
        paymentId:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/PaymentStatus'
        version:
          type: integer
          format: int64
        reviewedAt:
          type: string
          format: date-time
    AdminMemberResponse:
      type: object
      required: [id, kakaoProfileName, displayNickname, role, status, onboardingCompleted, createdAt, updatedAt, version]
      properties:
        id:
          type: integer
          format: int64
        kakaoProfileName:
          type: string
        name:
          type: string
        part:
          $ref: '#/components/schemas/MemberPart'
        displayNickname:
          type: string
        role:
          $ref: '#/components/schemas/MemberRole'
        status:
          $ref: '#/components/schemas/MemberStatus'
        onboardingCompleted:
          type: boolean
        approvedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    AdminMemberActionRequest:
      type: object
      required: [version]
      properties:
        version:
          type: integer
          format: int64
          minimum: 0
    AdminMemberRoleRequest:
      type: object
      required: [role, version]
      properties:
        role:
          $ref: '#/components/schemas/MemberRole'
        version:
          type: integer
          format: int64
          minimum: 0
    PaymentSettingRequest:
      type: object
      required: [bankName, accountNumber, accountHolder]
      properties:
        bankName:
          type: string
          maxLength: 100
        accountNumber:
          type: string
          maxLength: 100
        accountHolder:
          type: string
          maxLength: 100
        kakaoPayReceiveUrl:
          type: string
          maxLength: 500
    PaymentSettingResponse:
      type: object
      required: [id, bankName, accountNumber, accountHolder, active, createdBy, createdAt]
      properties:
        id:
          type: integer
          format: int64
        bankName:
          type: string
        accountNumber:
          type: string
        accountHolder:
          type: string
        kakaoPayReceiveUrl:
          type: string
        active:
          type: boolean
        createdBy:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
    NotificationItem:
      type: object
      required: [id, title, body, createdAt]
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        body:
          type: string
        linkUrl:
          type: string
        readAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
    NotificationResponse:
      type: object
      required: [items, unreadCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NotificationItem'
        unreadCount:
          type: integer
          format: int64
    Problem:
      type: object
      required: [title, status, code, detail, timestamp]
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        code:
          type: string
        detail:
          type: string
        instance:
          type: string
        timestamp:
          type: string
          format: date-time
        fieldErrors:
          type: array
          items:
            type: object
            required: [field, reason]
            properties:
              field:
                type: string
              reason:
                type: string
